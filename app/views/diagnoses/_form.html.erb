<%= form_with(model: diagnosis, local: true) do |form| %>
  <% if diagnosis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diagnosis.errors.count, "error") %> prohibited this diagnosis from being saved:</h2>

      <ul>
      <% diagnosis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :coding_system %>
    <%= form.text_field :coding_system, class: 'form-control', required: true %>
  </div>
  <div class="form-group">
    <%= form.label :code %>
    <%= form.text_field :code, class: 'form-control', required: true %>
  </div>
  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_field :description, class: 'form-control', required: true %>
  </div>
  <div class="actions">
    <%= form.submit 'Save Diagnosis', class: 'btn btn-primary' %>
  </div>
<% end %>
