<%= form_with(model: medication_route, local: true) do |form| %>
  <% if medication_route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medication_route.errors.count, "error") %> prohibited this medication_route from being saved:</h2>

      <ul>
      <% medication_route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control', required: true %>
  </div>

  <div class="actions">
    <%= form.submit 'Save Medication Route', class: 'btn btn-primary' %>
  </div>
<% end %>
